<article>
  <h2>{{title}}</h2>
  <p>Written by {{user.username}} on {{created_at}}</p>
  {{{markdownToHtml content}}}
  {{#each comments as |comment|}}
    <div>
      <p>{{user.username}} said on {{created_at}}</p>
      <p>{{comment_text}}</p>
    </div>
  {{/each}}
</article>