<section>
  <h2>My Posts</h2>
  <a href="/post/new">New Post</a>
  {{#if posts}}
  <ul>
    {{#each posts as |post|}}
        {{> post-preview post}}
    {{/each}}
  </ul>
  {{else}}
    <div>You haven't posted anything yet!</div>
  {{/if}}

</section>